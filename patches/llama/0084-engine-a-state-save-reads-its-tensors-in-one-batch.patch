diff --git a/ggml/include/ggml-backend.h b/ggml/include/ggml-backend.h
--- a/ggml/include/ggml-backend.h
+++ b/ggml/include/ggml-backend.h
@@ -85,6 +85,9 @@
 
     GGML_API void ggml_backend_tensor_set_async   (ggml_backend_t backend,       struct ggml_tensor * tensor, const void * data, size_t offset, size_t size);
     GGML_API void ggml_backend_tensor_get_async   (ggml_backend_t backend, const struct ggml_tensor * tensor,       void * data, size_t offset, size_t size);
+    // group the device-to-host reads between begin and end into as few waits as the backend allows
+    GGML_API void ggml_backend_read_batch_begin(ggml_backend_t backend);
+    GGML_API void ggml_backend_read_batch_end  (ggml_backend_t backend);
     GGML_API void ggml_backend_tensor_set_2d_async(ggml_backend_t backend,       struct ggml_tensor * tensor, const void * data, size_t offset, size_t size, size_t n_copies, size_t stride_tensor, size_t stride_data);
     GGML_API void ggml_backend_tensor_get_2d_async(ggml_backend_t backend, const struct ggml_tensor * tensor,       void * data, size_t offset, size_t size, size_t n_copies, size_t stride_tensor, size_t stride_data);
 
diff --git a/ggml/src/ggml-backend.cpp b/ggml/src/ggml-backend.cpp
--- a/ggml/src/ggml-backend.cpp
+++ b/ggml/src/ggml-backend.cpp
@@ -273,6 +273,18 @@
         ggml_backend_tensor_set(tensor, data, offset, size);
     } else {
         backend->iface.set_tensor_async(backend, tensor, data, offset, size);
+    }
+}
+
+void ggml_backend_read_batch_begin(ggml_backend_t backend) {
+    if (backend && backend->iface.read_batch_begin) {
+        backend->iface.read_batch_begin(backend);
+    }
+}
+
+void ggml_backend_read_batch_end(ggml_backend_t backend) {
+    if (backend && backend->iface.read_batch_end) {
+        backend->iface.read_batch_end(backend);
     }
 }
 
diff --git a/ggml/src/ggml-metal/ggml-metal-device.m b/ggml/src/ggml-metal/ggml-metal-device.m
--- a/ggml/src/ggml-metal/ggml-metal-device.m
+++ b/ggml/src/ggml-metal/ggml-metal-device.m
@@ -971,6 +971,7 @@
     id<MTLBlitCommandEncoder>  stage_batch_enc;
     size_t                     stage_batch_used;
     int                        stage_batch_n;
+    bool                       stage_batch_owned; // reopened inside a get: retained, released on flush
     struct { void * dst; size_t off; size_t size; } stage_batch_items[16];
 
     // cached no-copy wraps of host memory regions used as blit sources, so
@@ -3286,6 +3287,11 @@
         memcpy(dev->stage_batch_items[i].dst,
                (char *) dev->stage_buf.contents + dev->stage_batch_items[i].off,
                dev->stage_batch_items[i].size);
+    }
+    if (dev->stage_batch_owned) {
+        [dev->stage_batch_enc release];
+        [dev->stage_batch_cmd release];
+        dev->stage_batch_owned = false;
     }
     dev->stage_batch_n    = 0;
     dev->stage_batch_used = 0;
@@ -3312,6 +3318,11 @@
             ggml_metal_device_stage_batch_flush(dev);
         } else {
             [dev->stage_batch_enc endEncoding];
+            if (dev->stage_batch_owned) {
+                [dev->stage_batch_enc release];
+                [dev->stage_batch_cmd release];
+                dev->stage_batch_owned = false;
+            }
             dev->stage_batch_cmd = nil;
             dev->stage_batch_enc = nil;
         }
@@ -3319,6 +3330,14 @@
     [dev->stage_lock unlock];
 }
 
+static void ggml_metal_device_stage_batch_flush(ggml_metal_device_t dev);
+
+// a read batch drains every few MiB: larger batches wait less on a desktop host but copy out
+// of the staging buffer slower on a Xeon W one, and 4 MiB was the size that won on both
+static size_t ggml_metal_stage_batch_cap(void) {
+    return (size_t) 4*1024*1024;
+}
+
 bool ggml_metal_device_stage_get(ggml_metal_device_t dev, struct ggml_metal_buffer_id bid_src, void * data, size_t size) {
     if (size == 0) {
         return true;
@@ -3333,11 +3352,19 @@
 
     // inside an open read batch: queue the blit, defer the wait to batch end
     if (dev->stage_batch_cmd != nil) {
-        if (dev->stage_batch_n == 16 || dev->stage_batch_used + size > GGML_METAL_STAGE_BUF_MAX ||
-            ggml_metal_device_stage_buf(dev) == nil) {
+        if (ggml_metal_device_stage_buf(dev) == nil) {
             [dev->stage_lock unlock];
-            return false;   // full batch or no buffer: caller falls back to the direct path
+            return false;   // no buffer: caller falls back to the direct path
         }
+        // a full batch drains and reopens, so a long run of reads still pays one wait per
+        // staging buffer instead of one per read
+        if (dev->stage_batch_n == 16 || dev->stage_batch_used + size > ggml_metal_stage_batch_cap()) {
+            ggml_metal_device_stage_batch_flush(dev);
+            // the caller sits in an autorelease pool that drains before the batch ends
+            dev->stage_batch_cmd   = [[dev->mtl_queue commandBufferWithUnretainedReferences] retain];
+            dev->stage_batch_enc   = [[dev->stage_batch_cmd blitCommandEncoder] retain];
+            dev->stage_batch_owned = true;
+        }
         [dev->stage_batch_enc copyFromBuffer:bid_src.metal
                                 sourceOffset:bid_src.offs
                                     toBuffer:dev->stage_buf
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -2751,12 +2751,23 @@
 class llama_io_write_host : public llama_io_write_i {
 public:
     llama_io_write_host(
-            uint8_t * p, size_t len) : ptr(p), buf_size(len) {}
+            uint8_t * p, size_t len, const std::vector<ggml_backend_ptr> * backends = nullptr) : ptr(p), buf_size(len), backends(backends) {}
 
     ~llama_io_write_host() {
-        // TODO: add backend support to batch tensor_get? or some other way to speed this up
+        // on a discrete GPU each read is a round trip over the bus; batching them leaves one
+        // wait per staging buffer instead of one per tensor
+        if (backends) {
+            for (const auto & b : *backends) {
+                ggml_backend_read_batch_begin(b.get());
+            }
+        }
         for (const auto & winfo : winfos) {
             ggml_backend_tensor_get(winfo.tensor, winfo.ptr, winfo.offset, winfo.size);
+        }
+        if (backends) {
+            for (const auto & b : *backends) {
+                ggml_backend_read_batch_end(b.get());
+            }
         }
     }
 
@@ -2799,6 +2810,7 @@
         size_t offset;
     };
     std::vector<write_info> winfos;
+    const std::vector<ggml_backend_ptr> * backends;
 };
 
 class llama_io_read_host : public llama_io_read_i {
@@ -3216,7 +3228,7 @@
 }
 
 size_t llama_context::state_get_data(uint8_t * dst, size_t size) {
-    llama_io_write_host io(dst, size);
+    llama_io_write_host io(dst, size, &backends);
     try {
         return state_write_data(io);
     } catch (const std::exception & err) {
@@ -3255,7 +3267,7 @@
     if (flags & LLAMA_STATE_SEQ_FLAGS_ON_DEVICE) {
         io = std::make_unique<llama_io_write_device>(dst, size, mem_storage[seq_id]);
     } else {
-        io = std::make_unique<llama_io_write_host>(dst, size);
+        io = std::make_unique<llama_io_write_host>(dst, size, &backends);
     }
 
     try {
